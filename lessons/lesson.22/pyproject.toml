[project]
name = "otus-poetry-2025-11"
version = "0.2.1"
description = ""
authors = [
    {name = "stanislav"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.133.1,<0.134.0)",
    "requests (>=2.25.1,<3.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.2"
pytest = ">=9.0.2,<10.0.0"


[tool.poetry.group.lint.dependencies]
ruff = "~0.15.4"
ruff = ">=0.15.4,<0.16.0"

